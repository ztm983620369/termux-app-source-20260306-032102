你正在总结一段对话，以便之后继续工作时保留上下文。

**关键**：这份总结将是对话恢复时唯一可用的上下文。假设之前的所有消息都会丢失。请写得充分、具体。

**必须包含的部分**：

## 当前状态

- 当前正在处理的任务（用户原始请求要准确）
- 当前进度以及已经完成的内容
- 现在正在处理但尚未完成的内容
- 还需要做什么（给出具体下一步，不要含糊）

## 文件与改动

- 已修改的文件（简要说明改动）
- 已阅读/分析的文件（说明相关原因）
- 尚未处理但后续可能需要修改的关键文件
- 重要代码位置的文件路径和行号

## 技术上下文

- 已做出的架构决定以及原因
- 正在遵循的模式（带示例）
- 使用的库/框架
- 成功执行过的命令（写出精确命令和上下文）
- 失败过的命令（说明尝试内容以及失败原因）
- 环境细节（语言版本、依赖等）

## 策略与方法

- 当前采用的总体方法
- 选择该方法的原因
- 已发现的关键细节或注意事项
- 已做出的假设
- 已识别的阻塞点或风险

## 精确下一步

写具体事项。不要写“实现认证”，而要写成：

1. 在 src/middleware/auth.js:15 添加 JWT 中间件
2. 更新 src/routes/user.js:45 的登录处理，返回 token
3. 使用 `npm test -- auth.test.js` 测试

**语气**：像在给中途接手的队友交接。包含对方无需追问即可继续所需的一切信息。不要使用 emoji。

**长度**：不设上限。宁可多写细节，也不要漏掉关键上下文。
