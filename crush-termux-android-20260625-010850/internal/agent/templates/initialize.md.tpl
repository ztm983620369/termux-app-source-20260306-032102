分析这个代码库，并创建或更新 **{{.Config.Options.InitializeAs}}**，帮助后续 agent 在此仓库中高效工作。

**第一步**：检查目录是否为空，或是否只包含配置文件。如果是，请停止并回复：“目录看起来为空，或只包含配置。请先添加源码，然后再运行此命令生成 {{.Config.Options.InitializeAs}}。”

**目标**：记录 agent 在此代码库中工作所需了解的内容，包括命令、模式、约定、注意事项、整体架构以及组件如何协作。

**发现流程**：

1. 使用 `ls` 检查目录内容。
2. 查找已有规则文件（`.cursor/rules/*.md`、`.cursorrules`、`.github/copilot-instructions.md`、`claude.md`、`agents.md`），仅在文件存在时读取。
3. 根据配置文件和目录结构识别项目类型。
4. 从配置文件、脚本、Makefile 或 CI 配置中找出 build/test/lint 命令。
5. 阅读有代表性的源码文件，理解代码模式、架构、控制流和数据流。
6. 如果 {{.Config.Options.InitializeAs}} 已存在，读取并改进它。

**应包含的内容**：

- 关键命令（build、test、run、deploy 等），只写与项目相关的命令。
- 代码组织结构、应用架构、控制流和数据流。
- 命名约定和风格模式。
- 测试方法和测试模式。
- 重要注意事项或不明显的模式。
- 已有规则文件中的项目专属上下文。

**注意**：LLM agent 会在读取上下文时学习并适应，因此不要记录读一两个文件就能立刻发现的显而易见细节。遵循渐进式披露原则，重点写能避免反复试错的非显而易见知识：注意事项、隐含约定、带有意外参数的命令，以及无法从单个文件中自明的上下文。

**格式**：使用清晰的 Markdown 小节。根据发现内容自行判断结构。优先完整性，包含 agent 工作所需的一切信息。

**关键**：只记录实际观察到的内容。不要编造命令、模式或约定。找不到的内容不要写。
