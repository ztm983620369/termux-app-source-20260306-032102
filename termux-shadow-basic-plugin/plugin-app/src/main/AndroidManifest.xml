<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="false"
        android:label="${shadowDisplayName}"
        android:supportsRtl="true"
        android:theme="${shadowApplicationTheme}">
        <activity
            android:name="${shadowActivityClassName}"
            android:exported="false"
            android:theme="${shadowActivityTheme}"
            android:screenOrientation="${shadowScreenOrientation}"
            android:windowSoftInputMode="${shadowSoftInputMode}"
            android:configChanges="${shadowConfigChanges}" />
    </application>
</manifest>
